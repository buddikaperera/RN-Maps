import React, {Component} from 'react';
import {View, Text} from 'react-native';

class PercentageCircle extends Component {
  constructor(props) {
    super(props);
    this.state = {};
  }

  render() {
    return (
      <View>
        <Text> PercentageCircle </Text>
      </View>
    );
  }
}

export default PercentageCircle;

// if (!state.isLoggedIn) {
//   return <Redirect to="/login" />;
// }

///ui https://reactnativeexample.com/beautiful-backgrounds-shapes-to-react-native-based-in-flex/
